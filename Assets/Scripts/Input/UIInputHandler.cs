public class UIInputHandler : BaseInputHandler
{
    public UIInputHandler(BaseSelectionEntity selection, IEntitySelectionUIController selectionUIController) :
        base(selection, selectionUIController)
    {
    }
}